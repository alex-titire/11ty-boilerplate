---
layout: 'base.njk'
---

<div class="py-24 sm:py-32">
    <div class="mx-auto max-w-7xl px-6 lg:px-8">
        <div class="mx-auto max-w-2xl text-center">
            <h2 class="text-3xl font-bold tracking-tight text-gray-900 dark:text-slate-100 sm:text-4xl">From the blog</h2>
            <p class="mt-2 text-lg leading-8 text-gray-600 dark:text-gray-300">Learn how to grow your business with our expert advice.</p>
        </div>
        <div class="mx-auto mt-16 grid max-w-2xl grid-cols-1 gap-x-8 gap-y-20 lg:mx-0 lg:max-w-none lg:grid-cols-3">
            
            {% for article in collections.blog | reverse -%}
            
            <article class="flex flex-col items-start justify-between">
                <div class="relative w-full">
                    <img src="{{ article.data.image }}" alt="{{ article.data.image_alt }}" class="aspect-[16/9] w-full rounded-2xl bg-gray-100 object-cover sm:aspect-[2/1] lg:aspect-[3/2]">
                    <div class="absolute inset-0 rounded-2xl ring-1 ring-inset ring-gray-900/10"></div>
                </div>
                <div class="max-w-xl">
                    <div class="group relative mt-5">
                        <h3 class="mt-3 text-lg font-semibold leading-6 text-gray-900 dark:text-gray-100 group-hover:text-gray-500">
                            <a href="{{ article.url }}">
                                <span class="absolute inset-0"></span>
                                {{ article.data.title }}
                            </a>
                        </h3>
                        <p class="mt-5 line-clamp-3 text-sm leading-6 text-gray-600 dark:text-gray-400">{{ article.data.short_description }}</p>
                    </div>
                    <div class="mt-8 flex items-center justify-between gap-x-4 text-xs">
                        <time datetime="{{ item.date }}" class="text-gray-500">{{ article.date | readableDate }}</time>
                        <a href="{{ article.url }}" class="relative z-10 rounded-full bg-gray-50 dark:bg-indigo-500/10 px-3 py-1.5 font-medium text-gray-600 dark:text-indigo-400 ring-1 ring-inset ring-gray-500/20 dark:ring-indigo-500/20 hover:bg-gray-100">read more &rarr;</a>
                    </div>
                </div>
            </article>
            
            {%- endfor %}
            
        </div>
    </div>
</div>